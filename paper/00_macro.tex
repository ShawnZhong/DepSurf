\usepackage{multirow}
\usepackage{bbding} % for star rating
\usepackage{stfloats} % to fix floats
\usepackage{graphicx} % for rotate

%%%
%%% Style
%%%

\usepackage{minted} % code formatting
% \usemintedstyle{vs}
% \usemintedstyle{emacs}
\usemintedstyle{perldoc}
\setminted{
  fontsize=\small,
  baselinestretch=1,
}
\setmintedinline{
  % fontsize=\normalsize,
}

% use a bolder font for \texttt{}
\renewcommand*{\ttdefault}{cmtt}

%%%
%%% Spacing
%%%

% set the space
\setlength{\abovecaptionskip}{4pt plus 2pt minus 2pt}

% \AtEndEnvironment{listing}{\vspace{-8pt}} 
\BeforeBeginEnvironment{minted}{\vspace{-0.75em}}
\AfterEndEnvironment{minted}{\vspace{-0.75em}}

% % Change the space between the figure/table and the text
% \setlength{\textfloatsep}{3pt plus 3pt minus 3pt}

% % Change the space among figures/tables
% \setlength{\floatsep}{0pt plus 3pt minus 3pt}


% change enumerate spacing
\usepackage{enumitem} % https://www.texnia.com/archive/enumitem.pdf
\setlist{itemsep=0pt, topsep=3pt, parsep=3pt}

% change caption font size
\usepackage[font=small]{caption}


%%%
%%% Macros
%%%

% define \minisec{} command to get rid of paragraph indentation and bold the title
\newcommand{\minisec}[1]{\vspace{0.25em}\noindent\textbf{#1. }}

% define some macros for todo and shawn
\newcommand{\todo}[1]{\textcolor{red}{TODO: #1}}
\newcommand{\shawn}[1]{\textcolor{red}{Shawn: #1}}
